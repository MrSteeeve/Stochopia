[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mirage-bench"
version = "0.2.0"
description = "MIRAGE：面向 LLM Agent 的结构化衍生品动态训练与评测环境"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "pyyaml",
    "rich",
    "python-dotenv",
    "tushare",
]

[project.scripts]
mirage-benchmark = "mirage.benchmark_cli:main"

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["mirage"]
